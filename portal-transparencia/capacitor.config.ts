import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.portaltransparencia.app',
  appName: 'portal-transparencia',
  webDir: 'dist'
};

export default config;
